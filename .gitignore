# Dependencies
node_modules/

# Build output
dist/
out/
build/
release/
.vite/
*.tsbuildinfo

# electron-vite writes a timestamped copy of the config beside it while
# bundling and does not always clean it up after a crash or a killed run.
electron.vite.config.*.mjs

# Design sources — Figma is the source of truth, the .fig export is a binary
# snapshot that bloats history and never merges.
*.fig

# Local env / secrets — the TMDB key lives in %APPDATA%/movie-app/config.json,
# never in the repo.
.env
.env.*
*.local

# Logs
*.log
npm-debug.log*

# Editors
.vscode/
.idea/
*.swp

# OS
Thumbs.db
desktop.ini
.DS_Store

# Test / scan output
coverage/
/tmp-reports/
